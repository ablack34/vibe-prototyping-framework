# Session tracking (engagement state, research, plans)
.copilot-tracking/

# Node
node_modules/
dist/
.vite/
*.tsbuildinfo

# .NET
bin/
obj/
*.user
*.suo

# IDE
.vs/
*.swp
.idea/

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
*.local

# Azure
.azure/

# VIBE Web Surface — local pointer store (per-user, not the source of truth)
surface/.engagements.json
# VIBE Web Surface — local access store (allow-list + admins) when running in
# legacy/local mode with the default STORE path; runtime state, not source of truth
surface/access.json

# VIBE runner scratch (composed invocation)
.vibe/
